name: snmp
namespace: monitoring
syncWave: "2"
path: workloads/snmp
ignoreDifferences:
  - group: monitoring.coreos.com
    kind: Probe
    jqPathExpressions:
      - .spec.targets.staticConfig.relabelingConfigs[].action
