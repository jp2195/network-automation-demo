# Generated by tools/render — do not edit. Source: spec/atlanta.yaml + tools/render/*.
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: snmp-frr-cabinets
  labels:
    release: kps
spec:
  jobName: snmp-frr-cabinets
  interval: 300s
  scrapeTimeout: 25s
  module: if_mib
  prober:
    url: snmp-exporter.monitoring.svc.cluster.local:9116
    path: /snmp
  targets:
    staticConfig:
      # Targets the standard SNMP port (161/udp). See snmp_render.go's
      # FRRSNMPTargets for the FQDN derivation.
      static:
        - atlanta-fc-n.clabernetes.svc.cluster.local:161
        - atlanta-fc-nw.clabernetes.svc.cluster.local:161
        - atlanta-fc-i20e.clabernetes.svc.cluster.local:161
        - atlanta-fc-sw.clabernetes.svc.cluster.local:161
      labels:
        telemetry_source: snmp
        node_role: field-cabinet
      relabelingConfigs:
        # Promote the cabinet hostname from the target FQDN into a
        # `node` label so it joins with link_membership_info.
        - sourceLabels: [__param_target]
          regex: "atlanta-(fc-[a-z0-9]+)\\.clabernetes\\.svc\\.cluster\\.local:[0-9]+"
          targetLabel: node
          replacement: "$1"
        - targetLabel: __param_auth
          replacement: public_v2
