apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - deployment.yaml
  - service.yaml
  - servicemonitor.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: gnmic-config
    files:
      - gnmic.yaml=targets.yaml
