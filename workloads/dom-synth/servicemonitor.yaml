apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: dom-synth
  labels:
    app.kubernetes.io/name: dom-synth
    release: kps
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: dom-synth
  endpoints:
    - port: prom
      interval: 15s
      path: /metrics
